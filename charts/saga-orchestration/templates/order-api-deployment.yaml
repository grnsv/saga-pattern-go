apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "saga-orchestration.fullname" . }}-order-api
  labels:
    {{- include "saga-orchestration.labels" . | nindent 4 }}
    app.kubernetes.io/name: order-api
spec:
  replicas: {{ .Values.orderApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: order-api
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: order-api
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      initContainers:
        {{- include "saga-orchestration.kafkaWaitTopicsContainer" . | nindent 8 }}
      containers:
        - name: order-api
          image: "{{ .Values.orderApi.image }}:{{ .Values.orderApi.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.orderApi.port }}
          envFrom:
            - configMapRef:
                name: {{ include "saga-orchestration.fullname" . }}-config
          env:
            - name: HTTP_PORT
              value: {{ .Values.orderApi.port | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.orderApi.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
