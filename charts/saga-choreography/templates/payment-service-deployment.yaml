apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "saga-choreography.fullname" . }}-payment-service
  labels:
    {{- include "saga-choreography.labels" . | nindent 4 }}
    app.kubernetes.io/name: payment-service
spec:
  replicas: {{ .Values.paymentService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-service
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      initContainers:
        {{- include "saga-choreography.kafkaWaitTopicsContainer" . | nindent 8 }}
      containers:
        - name: payment-service
          image: "{{ .Values.paymentService.image }}:{{ .Values.paymentService.tag }}"
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: {{ .Values.paymentService.port }}
          envFrom:
            - configMapRef:
                name: {{ include "saga-choreography.fullname" . }}-config
          env:
            - name: HTTP_PORT
              value: {{ .Values.paymentService.port | quote }}
            - name: SUCCESS_RATE
              value: {{ .Values.paymentService.successRate | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.paymentService.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
